import { Suspense } from 'react'; import { AuthScreen } from '@/components/screens/auth/PasswordAuthScreens'; export default function Page(){return <Suspense><AuthScreen mode="register"/></Suspense>}
